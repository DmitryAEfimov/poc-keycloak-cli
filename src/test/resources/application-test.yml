spring:
  datasource:
    url: jdbc:h2:mem:testdb;LOCK_TIMEOUT=10000;MODE=PostgreSQL;DB_CLOSE_DELAY=2
    platform: h2
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        hbm2dll.create_namespaces: true